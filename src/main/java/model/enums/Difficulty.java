package model.enums;

public enum Difficulty {
    /**
     * enum of Difficulty settings. This then influences the size
     * of the minefield and the number of bombs placed.
     */
    UNKNOWN,
    EASY,
    NORMAL,
    HARD
}
